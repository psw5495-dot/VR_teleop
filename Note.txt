# 기존 설치 확인
ls ~/ros2_ws/src/unitree_ros2 2>/dev/null && echo "✅ 있음" || echo "❌ 설치 필요"

# 없으면 설치
cd ~/ros2_ws/src
git clone https://github.com/unitreerobotics/unitree_ros2.git
cd ~/ros2_ws
colcon build --packages-select unitree_ros2
source install/setup.bash
